@model gazprom.Models.FuelTank

@{
    ViewData["Title"] = "Подробная информация";
}

<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Id)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Id)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Size)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Size)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Value)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Value)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Fuel.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Fuel.Name)
        </dd>
        <dt>
            Диаграмма
        </dt>
        <dd>
            <canvas id="myChart" data-fuel-tank-size="@Model.Size" data-fuel-tank-value="@Model.Value" style="width: 100%; height: 500px;"></canvas>
        </dd>
    </dl>
</div>
<div>
    @if (User.IsInRole("admin"))
    {
        <a asp-action="Edit" asp-route-id="@Model.Id">Редактировать</a><text> | </text>
    }
    <a asp-action="Index">Назад</a>
</div>
